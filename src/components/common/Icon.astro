---
export interface Props {
  className?: string; // Optional classes for styling
  name: string; // Name of the icon (e.g., 'home', 'menu')
}
const { className = '', name } = Astro.props;
---

<svg aria-hidden="true" class={`icon ${className}`}>
  <use href={`/icons.svg#${name}`}></use>
</svg>
