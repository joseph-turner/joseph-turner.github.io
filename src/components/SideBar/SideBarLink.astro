---
const { href, id } = Astro.props;
const isActive = id === Astro.props.activeId;
const activeClass = " bg-base-300"; // For primary color replace with `active` class
---

<li><a class={`py-3 text-base${isActive ? activeClass : ''}`} id={id} href={href}><slot /></a></li>
