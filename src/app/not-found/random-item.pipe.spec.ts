import { RandomItemPipe } from './random-item.pipe';

describe('RandomItemPipe', () => {
  it('create an instance', () => {
    const pipe = new RandomItemPipe();
    expect(pipe).toBeTruthy();
  });
});
